The LIGO interferometers use a system called \gls{cds} to acquire data and control the interferometer. A digital signal processing system built to the same standards as \gls{cds} is available at Glasgow and was used to conduct some of the experiments reported on in this thesis. As \gls{cds} is a digital system, complex servo shaping can be implemented with relative ease, e.g.~the servo shown in Figure~\ref{fig:mzchapterservotransferfunction} was created with \gls{cds}. Additionally, processes such as locking a cavity on resonance, or locking an interferometer to the desired operating point, can be automated with digital logic, as signals can be monitored to trigger events. \gls{cds} can interface with a suite of software that makes interacting with an experiment easier; an example of \gls{cds} being used with the Motif Editor and Display Manager (MEDM) software~\cite{MEDM} to lock the \gls{pmc} (see Chapter~\ref{chapter:TnT_lab_work}) is shown in Figure~\ref{fig:medmscreen}.
\begin{figure}
	\centering
	\includegraphics[width=0.9\linewidth]{Figures/medm_screen}
	\caption[An example of a MEDM screen used to interface with CDS.]{This MEDM interface is used by operators of the interferometer to control and monitor the status of the \gls{pmc}. Several inputs and outputs can be monitored simultaneously, and locking the \gls{pmc} is reduced to simply pressing a button (\texttt{LOCK}). Most aspects of the locking servo can be adjusted with \gls{cds}, e.g. the \texttt{gain} slider adjusts the gain of the servo, thus operators have a large amount of freedom when they are trying to optimise the \gls{pmc}'s locking capabilities.}
	\label{fig:medmscreen}
\end{figure}
\par
\gls{cds} contains an array of \glspl{adc} for accepting analogue signals and \glspl{dac} for generating analogue outputs. An \gls{adc} measures a voltage, and the smallest voltage that it can measure is ultimately determined by the number of bits it uses. The smallest voltage measurable with the \gls{adc} corresponds to one count within \gls{cds}. 
\par
The Glasgow \gls{cds} uses \glspl{adc} with a voltage range of $\pm10\,\si{\volt}$, and the sample rate of the \glspl{adc} is 65\,kHz. The voltage is recorded as a signed 15 bit integer, so $2^{15}$ counts is equivalent to 10 Volts. Note that counts can exceed $2^{15}$ within the digital part of \gls{cds}. The digitised analogue input signals have a noise floor, and a measurement of this is shown in Figure~\ref{fig:mzchaptercdsnoise}. As digitally measured signals can be aliased if they exceed the bandwidth of the \glspl{adc}, signals that are measured with \gls{cds} are low passed at 9\,kHz with a third order filter. The noise of the Glasgow \gls{cds} analogue inputs is equivalent to an \gls{adc} with a 14 bit resolution.
\begin{figure}
	\centering
	\includegraphics[width=1\linewidth]{Figures/MZ_chapter_CDS_noise}
	\caption[Noise floor of the digitised input signals recorded by the Glasgow CDS.]{Noise floor of the digitised input signals recorded by the Glasgow \gls{cds}. This was converted into a voltage using $2^{15}$ counts per 10 Volts. The ripples above 1\,kHz arise due to the 3rd order low pass filter at 9\,kHz. Below $\sim10$\,Hz, the input noise rises as $1/f$.}
	\label{fig:mzchaptercdsnoise}
\end{figure}
